$engine: 3
$onesync: on
name: FDB-System-Base
version: 1.0.0
author: FarrofaDeBacon
description: Receita txAdmin automatizada para deploy completo da base FDB-System (RedM) com fdb-core, fdb-spawn, fdb-multicharacter, fdb-inventory e fdb-horses.

tasks:
  # 1. Dependências Oficiais Overextended (Release ZIP)
  - action: download_file
    path: ./tmp/files/oxmysql.zip
    url: https://github.com/overextended/oxmysql/releases/latest/download/oxmysql.zip
  - action: unzip
    dest: ./resources/[core]
    src: ./tmp/files/oxmysql.zip

  - action: download_file
    path: ./tmp/files/ox_lib.zip
    url: https://github.com/overextended/ox_lib/releases/latest/download/ox_lib.zip
  - action: unzip
    dest: ./resources/[core]
    src: ./tmp/files/ox_lib.zip

  # 2. Framework Core FDB-System
  - action: download_github
    src: FarrofaDeBacon/FDB-System
    ref: fix/companion-inventory-audit
    subpath: FDB-Core
    dest: ./resources/[fdb]/fdb-core

  # 3. Recursos Companheiros FDB-System
  - action: download_github
    src: FarrofaDeBacon/FDB-System
    ref: fix/companion-inventory-audit
    subpath: fdb-spawn
    dest: ./resources/[fdb]/fdb-spawn

  - action: download_github
    src: FarrofaDeBacon/FDB-System
    ref: fix/companion-inventory-audit
    subpath: fdb-multicharacter
    dest: ./resources/[fdb]/fdb-multicharacter

  - action: download_github
    src: FarrofaDeBacon/FDB-System
    ref: fix/companion-inventory-audit
    subpath: fdb-inventory
    dest: ./resources/[fdb]/fdb-inventory

  - action: download_github
    src: FarrofaDeBacon/FDB-System
    ref: fix/companion-inventory-audit
    subpath: fdb-horses
    dest: ./resources/[fdb]/fdb-horses

  # 4. Conexão e Criação do Banco de Dados MySQL
  - action: connect_database

  # 5. Geração do server.cfg
  - action: write_file
    file: ./server.cfg
    append: true
    data: |
      # ================================================
      #          FDB-System Base Configuration
      # ================================================

      # --- Identificação do Jogo (RedM) ---
      set gamename rdr3

      endpoint_add_tcp "0.0.0.0:30120"
      endpoint_add_udp "0.0.0.0:30120"

      # --- Conexão MySQL ---
      set mysql_connection_string "{{dbConnectionString}}"

      # --- Ordem de Inicialização dos Recursos ---
      ensure oxmysql
      ensure ox_lib
      ensure fdb-core
      ensure fdb-spawn
      ensure fdb-multicharacter
      ensure fdb-inventory
      ensure fdb-horses

      # --- Configurações Gerais do Servidor RedM ---
      sv_scriptHookAllowed 0
      sv_maxclients 48
      set sv_licenseKey "{{svLicense}}"
      set steam_webApiKey "{{steamApiKey}}"
