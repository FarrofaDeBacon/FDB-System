Config = {}

-- Tema Global da Interface (NUI)
-- Estes valores são injetados como variáveis CSS (--fdb-*) em todos os componentes Svelte.
Config.Theme = {
    accentColor = '#a87946', -- Dourado escuro / madeira velha
    backgroundColor = 'rgba(20, 15, 10, 0.98)', -- Fundo opaco, quase sem transparência moderna
    borderRadius = '2px', -- Bordas duras (sem cantos muito redondos)
    borderWidth = '2px', -- Borda mais espessa
    borderColor = '#4a3320', -- Borda escura estilo madeira
    font = '"Rye", serif', -- Fonte estilo faroeste (Rye)
    blurAmount = '0px', -- Sem desfoque (blur é moderno demais)
}
